add_namespace = MDMPAI
country_event = {
	id = aidisabler.1
	title = aidisabler.1.n
	desc = aidisabler.1.d
	picture = GFX_report_event_election_vote

	trigger = { 	
		NOT = { tag = ZZZ } 
		is_ai = yes
	}
	
	immediate = {  
		if = {
			limit = {
				has_game_rule = {
					rule = disable_ai_nations
					option = yes
				}
			}
			add_ideas = disabled_ai_nation
		}
	}	
}

country_event = {
	id = aidisabler.2
	title = "aidisabler.2.n"
	desc = "aidisabler.2.d"
	picture = GFX_report_event_election_vote

	trigger = { 	
		is_ai = no
		has_idea = disabled_ai_nation
	}
	
	immediate = {  
		remove_ideas = disabled_ai_nation
	}	
}
